pre-commit:
  commands:
    format:
      run: bun x ultracite fix
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      stage_fixed: true


pre-push:
  commands:
    push-to-codeup:
      run: git push codeup 
        
        